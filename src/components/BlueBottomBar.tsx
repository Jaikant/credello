import * as React from 'react';
import styled from 'styled-components';

const StyledBlueBtmBarContainer = styled.div`
  background: #02254d;
  height: 51px;
`;

export const BlueBtmBar: React.FC = () => {
  return <StyledBlueBtmBarContainer />;
};

export default BlueBtmBar;
